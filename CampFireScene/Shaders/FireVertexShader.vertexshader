#version 330 core

varying vec4 vColor;

void main()
{

	//float r = -(1/6) * gl_Vector.y + 1;
	//float g = 0;
	//float b = 0;
	//if(r < 0.5) {
	//	g = r;
	//}
	//if(r < 0.1) {
	//	b = r;
	//}

	//vColor = vec4(r, g, b, 0.0);
	vColor = vec4(0.0, 0.9, 0.0, 1.0);

	// Output position of the vertex, in clip space : MVP * position
	gl_Position = ftransform();
}